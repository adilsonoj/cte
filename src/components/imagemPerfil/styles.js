import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
        height: 60,
        width: 60,
        backgroundColor: '#000'
    },
});

export default styles;
