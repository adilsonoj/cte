import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
        height: 80,
        width: 80,
        borderRadius: 40,
        backgroundColor: '#000',
        marginBottom: 15
    },
});

export default styles;
